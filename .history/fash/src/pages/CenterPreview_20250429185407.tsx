import React from "react";

const CenterPreview = ({ selectedLesson, selectedSlide, slidePosition, goToNextSlide, sliderPosition, setIsDragging, comparisonContainerRef, sliderRef, activeSeriesIndex, setActiveSeriesIndex }) => {
  return (
    <div>
      {/* Placeholder for CenterPreview */}
      <h1>Center Preview</h1>
    </div>
  );
};

export default CenterPreview;