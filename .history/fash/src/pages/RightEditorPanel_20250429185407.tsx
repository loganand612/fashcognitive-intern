import React from "react";

const RightEditorPanel = ({ selectedSlide, handleUpdateSlide, handleUpdateListItem, handleAddListItem, handleRemoveListItem, handleFileUpload }) => {
  return (
    <div>
      {/* Placeholder for RightEditorPanel */}
      <h1>Right Editor Panel</h1>
    </div>
  );
};

export default RightEditorPanel;